# 验收清单（本轮）

| 项目 | 方式 | 结果 |
|---|---|---|
| 构建 / 类型 / lint | `next build`、`tsc --noEmit`、`eslint .` | 通过 |
| 数学一 / 数学二内容过滤 | `check:data`：数二队列不含 `scope:'m1'`、数一专属讲次只被数一卡引用、关键词黑名单；`check:schedule`：切换模式后新卡数、统计总数、日志隔离 | 通过 |
| 公式与 Markdown 渲染 | `check:data` 对全部公式 `throwOnError` 渲染；`check:render` 验证表格、编号步骤、块级公式滚动与放大结构 | 通过 |
| 翻面互斥显示 | `check:render`：hide 模式无答案样式、show 模式无空格、逐空揭示计数正确；学习界面答案区仅在 back 阶段渲染，正面不做 3D 叠层 | 通过 |
| 随机 / 顺序模式 | `check:schedule`：顺序按知识库索引单调、随机无相邻重复、答错卡至少间隔 3 张 | 通过 |
| 复习调度 | `check:schedule`：Good / Again 间隔关系、撤销回退日志、到期查询、每日新卡余额 | 通过 |
| 手机横竖屏 / 桌面布局 | 移动端底部标签栏 + 顶部安全区；桌面侧栏；学习页 `max-w-3xl` 居中，底部操作栏 sticky；块级公式容器横向滚动 | 代码审查 + curl 渲染 |
| 键盘与触控 | 空格 / 1–4 / H / F / U / I / Esc / Y / N / A–D / ←→；按钮最小 44px；`aria-pressed`、`aria-label`、`role=dialog`、焦点返回 | 代码审查 |
| 无水平溢出 | `html, body { overflow-x: hidden }`；`.mathtext { overflow-wrap: anywhere }`；行内 KaTeX 允许换行；块级公式与表格独立滚动 | 代码审查 |
| 离线与数据恢复 | Service Worker 预缓存应用壳与静态资源，导航离线回退；`check:schedule` 验证导出 / 导入往返、无效文件拒绝、旧版本迁移 | 通过 |
| 主要错误状态 | 存储写入失败横幅、导入失败提示、无效学习入口、空队列结束页、404 / 离线页 | 代码审查 |
